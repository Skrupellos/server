#include "main.h"

/**
 * The main function of the client application.
 * 
 * @param argc The number of parameters passed to the application on the
 *             command line (including the name of the application itself).
 * 
 * @param argv An array of the parameters passed to the application on the
 *             command line (including the name of the application itself).
 * 
 * @return The exit code of the application, indicating a success or failure.
 */
int main(int argc, char * argv[]) {
	ifmonMain(argc, argv);
}
